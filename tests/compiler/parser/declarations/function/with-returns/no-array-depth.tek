function foo(): Boolean
	one
	two

function foo(
): Boolean
	one
	two

function foo(
	): Boolean
	one
	two

function foo(
		): Boolean
	one
	two

function
foo(): Boolean
	one
	two

function
	foo(): Boolean
	one
	two

function
		foo(): Boolean
	one
	two

function
foo(
): Boolean
	one
	two

function
	foo(
	): Boolean
	one
	two

function
		foo(
		): Boolean
	one
	two

function
foo
(): Boolean
	one
	two

function
foo
	(): Boolean
	one
	two

function
foo
		(): Boolean
	one
	two

function
	foo
(): Boolean
	one
	two

function
		foo
(): Boolean
	one
	two

function
	foo
	(): Boolean
	one
	two

function
		foo
		(): Boolean
	one
	two

function
		foo
		(
		): Boolean
	one
	two

function
		foo
		(
): Boolean
	one
	two

function foo()
: Boolean
	one
	two

function
foo
()
: Boolean
	one
	two

function
	foo
	()
	: Boolean
	one
	two

function foo
()
: Boolean
	one
	two
