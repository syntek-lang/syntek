function foo(x: Number[][], y: String[][])
	one
	two

function
foo(x: Number[][], y: String[][])
	one
	two

function
	foo(x: Number[][], y: String[][])
	one
	two

function
		foo(x: Number[][], y: String[][])
	one
	two

function foo
(x: Number[][], y: String[][])
	one
	two

function foo
	(x: Number[][], y: String[][])
	one
	two

function foo
		(x: Number[][], y: String[][])
	one
	two

function
foo
(x: Number[][], y: String[][])
	one
	two

function
	foo
(x: Number[][], y: String[][])
	one
	two

function
		foo
(x: Number[][], y: String[][])
	one
	two

function
foo
	(x: Number[][], y: String[][])
	one
	two

function
foo
		(x: Number[][], y: String[][])
	one
	two

function
	foo
	(x: Number[][], y: String[][])
	one
	two

function
		foo
		(x: Number[][], y: String[][])
	one
	two

function foo(x: Number[][],
y: String[][])
	one
	two

function foo(
x: Number[][], y: String[][])
	one
	two

function foo(
x: Number[][], y: String[][],
)
	one
	two

function foo(
	x: Number[][],
	y: String[][],
)
	one
	two

function
foo
(
	x: Number[][],
	y: String[][],
)
	one
	two

function
	foo
	(x: Number[][], y: String[][],)
	one
	two

function
		foo
		(
			x: Number[][],
			y: String[][],
		)
	one
	two

function foo
		(
		x: Number[][],
		y: String[][],
		)
	one
	two

function foo
		(
x: Number[][],
y: String[][]
		)
	one
	two
