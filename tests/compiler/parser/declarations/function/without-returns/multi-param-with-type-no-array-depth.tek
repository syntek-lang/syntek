function foo(Number x, String y)
	one
	two

function
foo(Number x, String y)
	one
	two

function
	foo(Number x, String y)
	one
	two

function
		foo(Number x, String y)
	one
	two

function foo
(Number x, String y)
	one
	two

function foo
	(Number x, String y)
	one
	two

function foo
		(Number x, String y)
	one
	two

function
foo
(Number x, String y)
	one
	two

function
	foo
(Number x, String y)
	one
	two

function
		foo
(Number x, String y)
	one
	two

function
foo
	(Number x, String y)
	one
	two

function
foo
		(Number x, String y)
	one
	two

function
	foo
	(Number x, String y)
	one
	two

function
		foo
		(Number x, String y)
	one
	two

function foo(Number x,
String y)
	one
	two

function foo(
Number x, String y)
	one
	two

function foo(
Number x, String y,
)
	one
	two

function foo(
	Number x,
	String y,
)
	one
	two

function
foo
(
	Number x,
	String y,
)
	one
	two

function
	foo
	(Number x, String y,)
	one
	two

function
		foo
		(
			Number x,
			String y,
		)
	one
	two

function foo
		(
		Number x,
		String y,
		)
	one
	two

function foo
		(
Number x,
String y
		)
	one
	two
