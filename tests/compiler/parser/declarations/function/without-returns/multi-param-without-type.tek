function foo(x, y)
	one
	two

function
foo(x, y)
	one
	two

function
	foo(x, y)
	one
	two

function
		foo(x, y)
	one
	two

function foo
(x, y)
	one
	two

function foo
	(x, y)
	one
	two

function foo
		(x, y)
	one
	two

function
foo
(x, y)
	one
	two

function
	foo
(x, y)
	one
	two

function
		foo
(x, y)
	one
	two

function
foo
	(x, y)
	one
	two

function
foo
		(x, y)
	one
	two

function
	foo
	(x, y)
	one
	two

function
		foo
		(x, y)
	one
	two

function foo(x,
y)
	one
	two

function foo(
x, y)
	one
	two

function foo(
x, y,
)
	one
	two

function foo(
	x,
	y,
)
	one
	two

function
foo
(
	x,
	y,
)
	one
	two

function
	foo
	(x, y,)
	one
	two

function
		foo
		(
			x,
			y,
		)
	one
	two

function foo
		(
		x,
		y,
		)
	one
	two

function foo
		(
x,
y
		)
	one
	two

function
		foo
		(
		x
		,
		y
		,
		)
	one
	two
