function foo(x: Number[][])
	one
	two

function
foo(x: Number[][])
	one
	two

function
	foo(x: Number[][])
	one
	two

function
		foo(x: Number[][])
	one
	two

function foo
(x: Number[][])
	one
	two

function foo
	(x: Number[][])
	one
	two

function foo
		(x: Number[][])
	one
	two

function
foo
(x: Number[][])
	one
	two

function
	foo
(x: Number[][])
	one
	two

function
		foo
(x: Number[][])
	one
	two

function
foo
	(x: Number[][])
	one
	two

function
foo
		(x: Number[][])
	one
	two

function
	foo
	(x: Number[][])
	one
	two

function
		foo
		(x: Number[][])
	one
	two

function foo(
x: Number[][])
	one
	two

function foo(
x: Number[][]
)
	one
	two

function foo(
	x: Number[][]
)
	one
	two

function foo(
	x: Number[][]
	)
	one
	two

function
foo
(
x: Number[][]
)
	one
	two

function
foo
(
	x: Number[][]
)
	one
	two

function
foo
(
	x: Number[][])
	one
	two
